import * as models from '../models';
import { useModel } from '../../core';
import * as React from 'react';
export function Dashboard({ }) {
    const model = useModel(models.Dashboard);
    return <>sdfsdf</>
}